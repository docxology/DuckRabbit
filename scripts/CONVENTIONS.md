# Script conventions

- Resolve paths relative to the project root.
- Delegate all business logic to importable package functions.
- Keep commands deterministic and reproducible.
- Do not write generated artifacts outside the requested output directory.

