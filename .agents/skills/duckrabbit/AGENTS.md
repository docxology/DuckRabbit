# Local skill guidance

Keep this skill synchronized with the package’s typed registry and explicit
implementation-status policy. Avoid embedding generated counts or stale file
paths here; derive them from the registry when possible.
