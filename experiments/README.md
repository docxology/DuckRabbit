# experiments/

Experiment definitions/plans (see `experiment_plan.yaml` at the project root).

Contents: observer_protocol.md

Part of the DataTools lane (local-only, never committed). Parent: `../README.md`.
